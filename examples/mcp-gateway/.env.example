# Copy this to .env and `hush lock --rm` (optionally --bind-config), then point
# your AI tool's MCP config at `hush mcp`. Obviously-fake demo values — safe to
# commit (.env.example is not ignored; .env and *.hush are).
DATABASE_URL=postgres://demo:demo@localhost:5432/demo
API_KEY=demo-api-key-please-replace-1234567890
STRIPE_PUBLISHABLE_KEY=pk_test_demo_publishable_0000000000
STRIPE_SECRET_KEY=sk_test_demo_secret_DO_NOT_SHARE_00000
AWS_SECRET_ACCESS_KEY=demo/AWSsecret/replace/000000000000000
