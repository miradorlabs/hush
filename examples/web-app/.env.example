# Copy this to .env and put real (or fake) values here, then `hush lock --rm`.
# These are obviously-fake demo values — safe to commit (.env.example is not ignored).
PORT=3000
API_KEY=demo-api-key-please-replace-1234567890
DATABASE_URL=postgres://demo:demo@localhost:5432/demo
